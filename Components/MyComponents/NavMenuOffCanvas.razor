@namespace MyComponents.NavMenuOffCanvas
@rendermode InteractiveServer
@using MyComponents.NavigateTo



<img class="btn offcanvas-opener prevent-select" type="button"
    @onclick="() => {if (NavMenuCanvas != null) NavMenuCanvas.ShowAsync();}" aria-controls="offcanvasScrolling"
    style="display: @(isSplash ? "none" : "block")"
    src="/assets/S&V-Icons/sv-favicon.png" />

<Offcanvas @ref="NavMenuCanvas" Placement="Placement.Start" style="background-color: #33322a;">
    <BodyTemplate>
@*         <div class="prevent-select">
            <div class="accordion">
                <div class="accordion-item">
                    <h2 class="accordion-header">
                        <button class="accordion-button offcanvas-part" type="button" data-bs-toggle="collapse"
                                data-bs-target="#collapseOne" aria-expanded="false" aria-controls="collapseOne">
                            <h5>Where ya headed?</h5>
                        </button>
                    </h2>
                    <div id="collapseOne" class="accordion-collapse collapse">
                        <div class="accordion-body offcanvas-part">
                            <ul class="nav flex-column">
                                <NavigateTo link="svi-home" text="S&V - Indy" canvas=NavMenuCanvas/>
                                <NavigateTo link="svc-home" text="S&V - Cincy" canvas=NavMenuCanvas />
                                <NavigateTo link="cs-home" text="Comfort Station" canvas=NavMenuCanvas />
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
            <ul class="nav flex-column">
                <NavigateTo link="events" text="Events" canvas=NavMenuCanvas />
                <NavigateTo link="gallery" text="Gallery" canvas=NavMenuCanvas />
                <NavigateTo link="hours" text="Hours" canvas=NavMenuCanvas />
                <NavigateTo link="join-team" text="Join the Team" canvas=NavMenuCanvas />
            </ul>
        </div> *@
        <div class="prevent-select">
            <Accordion>
                <AccordionItem>
                    <TitleTemplate>
                        Where ya headed
                    </TitleTemplate>
                    <Content>
                            <ul class="nav flex-column">
                                <NavigateTo link="svi-home" text="S&V - Indy" canvas=NavMenuCanvas />
                                <NavigateTo link="svc-home" text="S&V - Cincy" canvas=NavMenuCanvas />
                                <NavigateTo link="cs-home" text="Comfort Station" canvas=NavMenuCanvas />
                            </ul>
                    </Content>
                </AccordionItem>
            </Accordion>
            <ul class="nav flex-column">
                <NavigateTo link="events" text="Events" canvas=NavMenuCanvas />
                <NavigateTo link="gallery" text="Gallery" canvas=NavMenuCanvas />
                <NavigateTo link="hours" text="Hours" canvas=NavMenuCanvas />
                <NavigateTo link="join-team" text="Join the Team" canvas=NavMenuCanvas />
            </ul>
        </div>
    </BodyTemplate>
</Offcanvas>

@code {
    [Parameter]
    public bool isSplash { get; set; } = true;

    public Offcanvas? NavMenuCanvas;
}